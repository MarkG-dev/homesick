# Secrets — never commit
.env

# API response cache (regenerable) and token accounting
.cache/
scripts/token_usage.jsonl

# Python
__pycache__/
*.pyc
.venv/
venv/

# run driver log (transient)
scripts/run_full.log
